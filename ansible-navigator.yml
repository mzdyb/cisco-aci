---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  execution-environment:
    image: hub.rh.lab/ee-network-aci:1.0
    pull:
      policy: missing
  playbook-artifact:
    enable: false
